/**
 * Canonical site origin. The SEO project publishes to neozino.dev, so every
 * absolute URL (metadata, canonicals, sitemap, JSON-LD, OpenGraph images)
 * resolves from this one constant. Change it here and the whole site follows.
 */
export const SITE_URL = "https://neozino.dev";
